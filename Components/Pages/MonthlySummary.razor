@page "/reports/monthly"

<PageTitle>ERPHub - Monthly Summary</PageTitle>

<AuthorizeView Context="authContext">
    <Authorized>
        <div class="animate-fade-in">
            <!-- Header Block -->
            <div class="flex justify-between items-center mb-6 max-sm:flex-col max-sm:items-start max-sm:gap-3">
                <div>
                    <h2 class="text-[22px] font-extrabold text-black tracking-tight font-heading">Monthly Summary</h2>
                    <p class="text-zinc-500 text-xs mt-0.5 font-body">Consolidated high-level performance metrics, work hour statistics, and operational audits.</p>
                </div>
            </div>

            <div class="bg-white border border-zinc-200 rounded-lg shadow-sm overflow-hidden">
                <div class="table-responsive-wrapper">
                    <table class="w-full">
                        <thead>
                            <tr>
                                <th>Metric Name <button class="sort-btn" @onclick="() => {}">↕</button></th>
                                <th>Current Value <button class="sort-btn" @onclick="() => {}">↕</button></th>
                                <th>Target / Limit <button class="sort-btn" @onclick="() => {}">↕</button></th>
                                <th>Monthly Growth <button class="sort-btn" @onclick="() => {}">↕</button></th>
                                <th>Variance Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (_metrics.Any())
                            {
                                @foreach (var item in _metrics)
                                {
                                    <tr>
                                        <td data-label="Metric Name">@item.MetricName</td>
                                        <td data-label="Current Value">@item.CurrentValue</td>
                                        <td data-label="Target / Limit">@item.Target</td>
                                        <td data-label="Monthly Growth">@item.Growth</td>
                                        <td data-label="Variance Status">
                                            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-[10px] font-bold @item.BadgeClass">@item.Status</span>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="5" class="text-center py-8 text-muted">Data Not Available</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="table-footer">
                    <div class="flex items-center gap-3">
                        <span class="pagination-info">Rows per page:</span>
                        <select class="page-size-select">
                            <option>10</option>
                            <option>25</option>
                            <option>50</option>
                        </select>
                    </div>
                    <div class="pagination">
                        <button class="pagination-btn" disabled>‹</button>
                        <button class="pagination-btn active">1</button>
                        <button class="pagination-btn" disabled>›</button>
                    </div>
                </div>
            </div>
        </div>
    </Authorized>
    <NotAuthorized>
        <RedirectToLogin />
    </NotAuthorized>
</AuthorizeView>

@code {
    public class KpiMetric
    {
        public string MetricName { get; set; } = string.Empty;
        public string CurrentValue { get; set; } = string.Empty;
        public string Target { get; set; } = string.Empty;
        public string Growth { get; set; } = string.Empty;
        public string Status { get; set; } = string.Empty;
        public string BadgeClass { get; set; } = string.Empty;
    }

    private List<KpiMetric> _metrics = new()
    {
        new KpiMetric { MetricName = "Total Work Hours", CurrentValue = "22,450 hrs", Target = "20,000 hrs Min", Growth = "↑ 4.2%", Status = "Optimal", BadgeClass = "bg-green-50 text-green-700" },
        new KpiMetric { MetricName = "Turnover Rate", CurrentValue = "0.85%", Target = "2.00% Max", Growth = "↓ 0.2%", Status = "Optimal", BadgeClass = "bg-green-50 text-green-700" },
        new KpiMetric { MetricName = "Payroll Budget Variance", CurrentValue = "$4,850.00", Target = "$5,000.00 Allow", Growth = "↑ 0.5%", Status = "Within Bounds", BadgeClass = "bg-green-50 text-green-700" }
    };
}
