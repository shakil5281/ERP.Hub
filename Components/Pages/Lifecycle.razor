@page "/lifecycle"

<PageTitle>ERPHub - System Lifecycle</PageTitle>

<AuthorizeView Context="authContext">
    <Authorized>
        <div class="animate-fade-in">
            <!-- Header Block -->
            <div class="flex justify-between items-center mb-6 max-sm:flex-col max-sm:items-start max-sm:gap-3">
                <div>
                    <h2 class="text-[22px] font-extrabold text-black tracking-tight font-heading">System Lifecycle</h2>
                    <p class="text-zinc-500 text-xs mt-0.5 font-body">Track, monitor, and audit system background services, scheduled jobs, and system events in real-time.</p>
                </div>
            </div>

            <div class="bg-white border border-zinc-200 rounded-lg shadow-sm overflow-hidden">
                <MudTable Items="@_events" Hover="true" SortLabel="Sort By" Dense="true" Striped="false" Outlined="false" Elevation="0">
                    <HeaderContent>
                        <MudTh><MudTableSortLabel SortBy="new Func<SystemEvent, object>(x=>x.EventName)">Event Name</MudTableSortLabel></MudTh>
                        <MudTh><MudTableSortLabel SortBy="new Func<SystemEvent, object>(x=>x.Details)">Details</MudTableSortLabel></MudTh>
                        <MudTh><MudTableSortLabel SortBy="new Func<SystemEvent, object>(x=>x.Timestamp)">Timestamp</MudTableSortLabel></MudTh>
                        <MudTh>Severity</MudTh>
                    </HeaderContent>
                    <RowTemplate>
                        <MudTd DataLabel="Event Name">@context.EventName</MudTd>
                        <MudTd DataLabel="Details">@context.Details</MudTd>
                        <MudTd DataLabel="Timestamp">@context.Timestamp</MudTd>
                        <MudTd DataLabel="Severity">
                            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-[10px] font-bold @context.BadgeClass">@context.Severity</span>
                        </MudTd>
                    </RowTemplate>
                    <PagerContent>
                        <MudTablePager PageSizeOptions="new int[] { 10, 25, 50 }" />
                    </PagerContent>
                <NoRecordsContent>
                    <MudText Typo="Typo.body2" Class="text-center mud-secondary-text-color py-8">Data Not Available</MudText>
                </NoRecordsContent>
                </MudTable>
            </div>
        </div>
    </Authorized>
    <NotAuthorized>
        <RedirectToLogin />
    </NotAuthorized>
</AuthorizeView>

@code {
    public class SystemEvent
    {
        public string EventName { get; set; } = string.Empty;
        public string Details { get; set; } = string.Empty;
        public string Timestamp { get; set; } = string.Empty;
        public string Severity { get; set; } = string.Empty;
        public string BadgeClass { get; set; } = string.Empty;
    }

    private List<SystemEvent> _events = new()
    {
        new SystemEvent { EventName = "Attendance Sync", Details = "Successfully pulled 452 raw punch logs from main biometric terminal.", Timestamp = "Just now", Severity = "Success", BadgeClass = "bg-green-50 text-green-700" },
        new SystemEvent { EventName = "DB Connection", Details = "Entity Framework SQL Server connection successfully pooled and validated.", Timestamp = "10 minutes ago", Severity = "Info", BadgeClass = "bg-green-50 text-green-700" },
        new SystemEvent { EventName = "Backup Complete", Details = "Database snapshot securely backed up to local storage volume.", Timestamp = "1 hour ago", Severity = "Warning", BadgeClass = "bg-amber-50 text-amber-700" }
    };
}
